# Supporting Source Note

This directory contains the supporting-section copy of `diagnose.rs`.

The authoritative buildable source used by the main crate is:

- `../../01_manuscript_direct_materials/code/src/diagnose.rs`

The duplicate copy here is kept only so the supporting section remains
self-explanatory when read on its own.
